function Header() {
  return (
    <header className="header">
      <div className="logo">Authentor</div>
      <button className="join-now">Join now</button>
    </header>
  );
}

export default Header;
